Bound of the values to find - The Go Programming Language Specification

variable with 1st letter capital is public variable.

package main

import "fmt"

// Only inside the method it is allowed
// jwtToken := 300000

// global
var jwtToken int = 3000

// constant and public variable
const LoginToken string = "hfj"

func main() {
	//var username string = "Prerna"
	//var username bool = true
	//var username uint8 = 255
	var username float32 = 255.233
	fmt.Println(username)
	fmt.Printf("Variable is of the type: %T \n", username)

	// default values and some alaises
	//var anotherVariable int
	var anotherVariable string
	fmt.Println(anotherVariable)
	fmt.Printf("Variable is of the type: %T \n", anotherVariable)

	// Implicit Variables - we can ignore the variable type and just give the value directly.
	var website = "google.com"
	fmt.Println(website)

	// no var style
	// Only inside the method it is allowed
	numberOfUsers := 300000
	fmt.Println(numberOfUsers)

}
